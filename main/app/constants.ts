export enum AppWindowName {
    Main = "main",
}

export enum RendererAppEntry {
    Index = "/home",
}

export enum AppStoreNamespace {
    WindowState = "window-state",
}
export enum WindowStateStoreKey {
    State = "window-state",
}